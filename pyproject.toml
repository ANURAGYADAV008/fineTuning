[project]
name = "finetuning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anurag Yadav", email = "jaysinghanurag321@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "tiktoken>=0.14.0",
    "torch>=2.13.0",
]

[project.scripts]
finetuning = "finetuning:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
